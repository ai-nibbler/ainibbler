# Base URL of the separate ai-project-pantry app (landing at /).
# Local: run `npm run dev` in ../ai-project-pantry and use the "Local:" URL from the terminal
# (often http://localhost:5173; if that port was taken, Vite picks the next port — paste it here).
# VITE_AI_PROJECT_PANTRY_URL=http://localhost:5173
